# Additional requirements for audio transcription feature
# Add these to your existing requirements.txt

# Audio transcription - FREE and open-source alternatives
faster-whisper
pydub

# Audio processing dependencies
ffmpeg-python

# Note: You also need to install ffmpeg system-wide (FREE)
# Ubuntu/Debian: sudo apt-get install ffmpeg
# macOS: brew install ffmpeg
# Windows: Download from https://ffmpeg.org/download.html

# Optional: For GPU acceleration (requires NVIDIA GPU + CUDA)
# The above packages work on CPU by default - no paid services needed!